import { ThreeDots } from 'react-loader-spinner';

const Loader = () => (
  <ThreeDots
    height="120"
    width="120"
    color="green"
    ariaLabel="three-dots-loading"
  />
);

export default Loader;
